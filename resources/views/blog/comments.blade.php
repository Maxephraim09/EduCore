@extends('layouts.app')

@section('title', 'Manage Blog Comments')

@section('content')
<div class="container-fluid"><div class="d-flex justify-content-between align-items-center mb-3"><div><h2 class="mb-1">Manage comments</h2><p class="text-muted mb-0">Approve, hide or delete public blog comments.</p></div><a href="{{ route('admin.blog.index') }}" class="btn btn-outline-secondary">Back to blog</a></div>@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif<div class="card shadow-sm"><div class="table-responsive"><table class="table mb-0 align-middle"><thead><tr><th>Comment</th><th>Post</th><th>Status</th><th class="text-end">Actions</th></tr></thead><tbody>@forelse($comments as $comment)<tr><td><strong>{{ $comment->name }}</strong><br>{{ Str::limit($comment->body, 180) }}<br><small class="text-muted">{{ $comment->email }}</small></td><td>{{ $comment->post->title ?? 'Deleted post' }}</td><td><span class="badge bg-{{ $comment->is_approved ? 'success' : 'warning' }}">{{ $comment->is_approved ? 'Approved' : 'Pending' }}</span></td><td class="text-end text-nowrap">@if(!$comment->is_approved)<form method="POST" action="{{ route('admin.blog.comments.approve', $comment) }}" class="d-inline">@csrf<button class="btn btn-sm btn-success">Approve</button></form>@else<form method="POST" action="{{ route('admin.blog.comments.reject', $comment) }}" class="d-inline">@csrf<button class="btn btn-sm btn-outline-warning">Hide</button></form>@endif<form method="POST" action="{{ route('admin.blog.comments.destroy', $comment) }}" class="d-inline" onsubmit="return confirm('Delete this comment?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger"><i class="fas fa-trash"></i></button></form></td></tr>@empty<tr><td colspan="4" class="text-center py-4">No comments yet.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $comments->links() }}</div></div>
@endsection
