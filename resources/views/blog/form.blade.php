@extends('layouts.app')

@section('title', $post->exists ? 'Edit Blog Post' : 'New Blog Post')

@section('content')
<div class="container-fluid" style="max-width: 1000px"><h2>{{ $post->exists ? 'Edit Blog Post' : 'Submit Blog Post' }}</h2><form method="POST" enctype="multipart/form-data" action="{{ $post->exists ? route('admin.blog.update', $post) : route('admin.blog.store') }}" class="card card-body shadow-sm">@csrf @if($post->exists) @method('PUT') @endif<div class="mb-3"><label class="form-label">Title</label><input name="title" class="form-control" value="{{ old('title', $post->title) }}" required></div><div class="row"><div class="col-md-6 mb-3"><label class="form-label">Category</label><select name="category_id" class="form-select"><option value="">Select category</option>@foreach($categories as $category)<option value="{{ $category->id }}" @selected(old('category_id', $post->category_id) == $category->id)>{{ $category->name }}</option>@endforeach</select></div><div class="col-md-6 mb-3"><label class="form-label">Tags</label><input name="tags" class="form-control" value="{{ old('tags', $post->tags->pluck('name')->implode(', ')) }}" placeholder="news, academics, sports"></div></div><div class="mb-3"><label class="form-label">Featured image</label><input name="featured_image" type="file" accept="image/jpeg,image/png,image/webp" class="form-control">@if($post->featured_image)<img src="{{ asset('storage/' . $post->featured_image) }}" alt="Current featured image" class="mt-2 rounded" style="max-width: 260px; max-height: 150px; object-fit: cover">@endif</div><div class="mb-3"><label class="form-label">Excerpt</label><textarea name="excerpt" class="form-control" rows="2">{{ old('excerpt', $post->excerpt) }}</textarea></div><div class="mb-3"><label class="form-label">Content</label><textarea name="content" class="form-control" rows="14" required>{{ old('content', $post->content) }}</textarea></div><div class="d-flex gap-2"><button class="btn btn-primary">{{ auth()->user()->role === 'teacher' ? 'Submit for approval' : 'Save post' }}</button><a href="{{ route('admin.blog.index') }}" class="btn btn-outline-secondary">Cancel</a></div></form></div>
@endsection